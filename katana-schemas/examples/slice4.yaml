---
_comment: This GST uses reference to a previously on-boarded slice_descriptor, but
  also defines some new values that will be used, replacing the ones defined on the
  referenced slice
slice_descriptor:
  slice_des: embb_ncsrd1
  network_DL_throughput:
    guaranteed: 10000
    maximum: 100000
  mtu: 500
